const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctsj/state': path.resolve(__dirname, 'src/ctsj-state'),
      //   },
      // },
    };
  },
};
